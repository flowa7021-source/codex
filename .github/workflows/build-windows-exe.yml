name: Build Windows App

on:
  workflow_dispatch:
  push:
    branches:
      - Final
      - main
      - master
  pull_request:
    branches:
      - Final
      - main
      - master

permissions:
  contents: read

concurrency:
  group: windows-build-${{ github.ref }}
  cancel-in-progress: true

env:
  SOURCE_PROJECT_DIR: G-claude-repair-ocr-bugs-gQxxl (1)/G-claude-repair-ocr-bugs-gQxxl/codex-codex-develop-modern-alternative-to-djvu-reader-uuxh9z/codex-codex-develop-modern-alternative-to-djvu-reader-uuxh9z
  BUILD_DIR: C:/nr-build

jobs:
  build:
    runs-on: windows-2022
    timeout-minutes: 60

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Prepare short build path
        shell: pwsh
        run: |
          if (Test-Path "${{ env.BUILD_DIR }}") {
            Remove-Item "${{ env.BUILD_DIR }}" -Recurse -Force
          }
          New-Item -ItemType Directory -Path "${{ env.BUILD_DIR }}" -Force | Out-Null
          robocopy "${{ env.SOURCE_PROJECT_DIR }}" "${{ env.BUILD_DIR }}" /MIR /NFL /NDL /NJH /NJS /NP /XD node_modules dist
          if ($LASTEXITCODE -gt 7) { throw "robocopy failed with exit code $LASTEXITCODE" }

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: ${{ env.SOURCE_PROJECT_DIR }}/package-lock.json

      - name: Install dependencies
        shell: pwsh
        working-directory: ${{ env.BUILD_DIR }}
        env:
          CYPRESS_INSTALL_BINARY: "0"
        run: |
          npm ci --no-audit --no-fund

      - name: Build portable EXE
        shell: pwsh
        working-directory: ${{ env.BUILD_DIR }}
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: "false"
        run: npm run build:win:nosign

      - name: Build installer EXE
        shell: pwsh
        working-directory: ${{ env.BUILD_DIR }}
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: "false"
        run: npm run build:win:installer:nosign

      - name: Validate build output
        shell: pwsh
        working-directory: ${{ env.BUILD_DIR }}
        run: |
          $portable = Get-ChildItem dist -Filter 'NovaReader-*.exe' -File | Where-Object { $_.Name -notlike 'NovaReader-Setup-*' }
          $installer = Get-ChildItem dist -Filter 'NovaReader-Setup-*.exe' -File
          if (!$portable) { throw 'Portable EXE not found' }
          if (!$installer) { throw 'Installer EXE not found' }
          if (!(Test-Path 'dist/win-unpacked/NovaReader.exe')) { throw 'win-unpacked/NovaReader.exe not found' }

      - name: Archive ready-to-run folder
        shell: pwsh
        working-directory: ${{ env.BUILD_DIR }}
        run: |
          Compress-Archive -Path 'dist/win-unpacked/*' -DestinationPath 'dist/NovaReader-win-unpacked.zip' -Force

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: novareader-windows-app
          path: |
            ${{ env.BUILD_DIR }}/dist/NovaReader-*.exe
            ${{ env.BUILD_DIR }}/dist/NovaReader-win-unpacked.zip
          if-no-files-found: error
          retention-days: 14
